import React from 'react';
import { useRef } from 'react';

import { cn } from '@/utils/cn';
import {
  motion,
  useAnimationFrame,
  useMotionTemplate,
  useMotionValue,
  useTransform,
} from 'framer-motion';

export const Button = ({
  borderRadius = '1.75rem',
  children,
  Component = 'button',
  containerClassName,
  borderClassName,
  duration,
  className,
}) => {};
